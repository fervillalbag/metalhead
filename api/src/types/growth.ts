import { Description } from "./description";

export type GrowthInfo = {
  id: string;
  title: string;
  description: Description[];
  createdAt: string;
};

export type GrowthHome = {
  id: string;
  title: string;
  description: Description[];
  createdAt: string;
};
